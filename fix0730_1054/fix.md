[Task] OMR 채점 프로그램 UI/UX 개선, 비동기 상태 반응형 UI 구현 및 전역 예외 처리/로깅 시스템 구축 (캡처 화면 기반)

다음 피드백 요청과 제공된 캡처 화면(image_0 ~ image_5)을 바탕으로 프로그램의 프론트엔드 UI 스타일링 버그 수정, 컴포넌트 표준화, 상태 표시 개선, 비동기 스레드 처리 및 전역 로깅 시스템을 구현해주세요.

---

### 1. 전반적인 UI/UX 개선 (image_0, image_3, image_5 기반)

*   **다크 테마 배경 버그 수정**: `image_0`의 도움말 버튼 컨테이너 및 왼쪽 사이드바 영역에 하얀색 배경이 노출되는 CSS 버그를 수정하여 다크 테마 일관성을 확보해 주세요.
*   **타이포그래피 및 강조 스타일 정돈**: `image_0` 및 `image_3`에서 확인되는 '시험명 입력', '인식 프로필', '프로필 끌어놓기' 텍스트의 과도한 하이라이트/텍스트 섀도 효과를 제거해 주세요. 가독성을 향상시키는 폰트 굵기(Font-weight)와 대비(Contrast) 중심으로 깔끔하게 재설정하여 '잘못 디자인된' 느낌을 없애주세요.
*   **버튼 Hover 상태 피드백 수정**:
    *   `image_1`의 '응답엑셀로시작' 버튼 마우스 오버 시 배경이 투명해져 흰색 글씨가 안 보이는 현상을 수정해 주세요. 오버 시에도 가독성을 확보하는 배경색을 설정해 주세요.
    *   'OMR인식 실행' 버튼 활성화 상태에서 마우스 오버 시 시각적 피드백(색상/투명도 변화 등)을 추가해 주세요.

*   **동적 프로그레스 UI 구현 (image_2 기반)**: 'OMR인식 실행' 버튼을 누를 때 하단 프로그레스 바(`image_2`)를 실시간 상태 텍스트(State)와 연동해 주세요. 원래 설계대로 아래와 같이 텍스트가 반응형으로 출력되어야 합니다.
    > 예시: `OMR인식 실행중. 경과 {elapsed_time}초. 남은시간 계산 중... ({current_index} / {total_count}) 인식중.`

---

### 2. 정답/채점 화면 개선 (image_3 기반)

*   **컴포넌트 표준화**: `image_3`에서 단순 텍스트/링크 형태로 되어 있는 '샘플 정답표 내려받기' 및 '정답표 엑셀 찾아보기' 텍스트 요소들을 표준 버튼(Button) 디자인 컴포넌트로 변경해 주세요.
*   **드래그 앤 드롭 영역 하단 버튼**: 드래그 앤 드롭 영역 하단의 '채점 실행' 버튼도 클릭 가능한 표준 버튼 스타일로 재설계해 주세요.

---

### 3. 시험 관리 화면 개선 (image_5 기반)

*   **행 선택(Row Selection) 동작 수정**: `image_5`의 '선택' 컬럼 또는 해당 행 클릭 시 체크박스(Checkbox) 토글 및 Row Active 상태(색상 전환)가 즉각 반영되도록 이벤트를 추가해 주세요.
*   **테이블 컬럼 레이아웃 유연화**: `image_5`에서 '시험명', '채점일시' 등 각 셀의 크기가 고정되어 텍스트가 잘리는 현상을 해결해 주세요. 컬럼 너비를 비율/가변(Flex/Auto) 처리하거나, 필요 시 텍스트 말줄임표(...) 및 툴팁(Tooltip) 노출, 컬럼 리사이즈(Column Resize) 기능을 추가해 주세요.
*   **액션 컴포넌트 버튼화**: '상세보기', '삭제' 텍스트를 클릭 피드백이 명확한 액션 버튼 컴포넌트로 변경해 주세요.

---

### 4. 프로그램 안정성 및 비동기 처리 개선 (image_0, image_5, image_2, image_4 기반)

*   **메인 스레드 블로킹(응답 없음) 예외 처리**: `image_5`에서 '시험 관리' 화면 진입 또는 데이터 조회 시 UI 메인 스레드가 멈추어 '응답 없음'이 발생하는 현상을 수정해 주세요. 데이터 입출력 및 무거운 로직을 비동기(Async/Await 또는 Worker Thread)로 전환하여 UI 반응성을 확보해 주세요.

*   **핵심 문제 해결: '채점 실행' 프로그레스 바 멈춤 문제 (image_4 기반)**: `image_4`의 채점 실행 프로그레스 바가 실제 처리 정도를 반영하지 못하고 멈춰 있다가 갑자기 완료되는 현상을 수정해 주세요. 채점 로직을 백그라운드 스레드로 전환하고, 처리 상태(퍼센트, 숫자)를 주기적으로 메인 UI 스레드에 전달하여 프로그레스 바가 실시간으로 움직이고 텍스트가 변하도록 비동기 로직 및 상태 업데이트 메커니즘을 수정해 주세요.

*   **전역 예외 처리 및 로컬 로그(Logging) 파일 생성**: 미처리 예외(Unhandled Exception) 발생 시 프로그램이 멈추거나 강제 종료되지 않도록 전역 에러 바운더리/Try-Catch를 구현해 주세요. 에러 발생 시 타임스탬프, 에러 위치, 스택 트레이스(Stack Trace)를 로컬 로그 파일(`logs/app_{YYYYMMDD}.log`)로 자동 기록하는 로깅 라이브러리/모듈을 적용해 주세요.